---
import { Play, ArrowRight, MessageCircle } from "lucide-astro";
---

<header
  id="inicio"
  class="relative h-screen min-h-[800px] flex items-center justify-center overflow-hidden"
>
  <!-- Cinematic Background -->
  <div class="absolute inset-0 z-0">
    <video
      id="hero-video"
      autoplay
      loop
      muted
      playsinline
      class="w-full h-full object-cover scale-105 md:scale-100"
      style="filter: brightness(0.6) contrast(1.1) grayscale(0.1);"
    >
      <source src="/videos/hero.mp4" type="video/mp4" />
      <!-- Fallback para navegadores antiguos -->
      Tu navegador no soporta videos HTML5.
    </video>
    <!-- Advanced Gradient Overlay -->
    <div class="absolute inset-0 bg-premium-gradient opacity-70"></div>
  </div>

  <div class="relative z-10 container mx-auto px-6 text-center pt-20">
    <!-- Floating Badge -->
    <div
      class="inline-flex items-center gap-3 px-6 py-2 rounded-full border border-white/10 bg-white/5 backdrop-blur-md text-brand-yellow font-bold text-xs uppercase tracking-[0.2em] mb-12 animate-fade-in-up"
    >
      <span
        class="w-2 h-2 rounded-full bg-brand-yellow animate-pulse-glow shadow-[0_0_10px_#FACC15]"
      ></span>
      Un gimnasio Diferente
    </div>

    <!-- Main Headline -->
    <h1
      class="text-5xl md:text-8xl lg:text-9xl font-heading font-black text-white leading-none mb-8 tracking-wide animate-fade-in-up delay-100 drop-shadow-2xl"
    >
      Sé parte<br />
      <span
        class="text-transparent bg-clip-text bg-gradient-to-r from-brand-yellow via-white to-brand-yellow"
        >de PLUS</span
      >
    </h1>

    <!-- Subtext -->
    <p
      class="text-xl md:text-2xl text-gray-300 max-w-2xl mx-auto mb-12 font-light leading-relaxed animate-fade-in-up delay-300 mix-blend-plus-lighter"
    >
      Seguimiento técnico constante en un ambiente diseñado para divertirte y
      ser tu mejor versión.
    </p>

    <!-- CTA Buttons -->
    <div
      class="flex flex-col md:flex-row gap-6 justify-center items-center animate-fade-in-up delay-500"
    >
      <a
        href="https://wa.me/59892744211?text=Hola!%20Me%20gustar%C3%ADa%20recibir%20m%C3%A1s%20informaci%C3%B3n%20sobre%20%2BFIT%20y%20reservar%20mi%20prueba%20gratis.%20Gracias!"
        class="group relative px-8 py-4 !bg-brand-yellow !text-black font-black uppercase tracking-widest rounded-full overflow-hidden transition-all hover:scale-105 hover:shadow-[0_0_40px_rgba(250,204,21,0.6)] ring-2 ring-transparent hover:ring-white/50"
      >
        <span class="relative z-10 flex items-center gap-2">
          Empezar Ahora <ArrowRight
            class="w-5 h-5 group-hover:translate-x-1 transition-transform stroke-2"
          />
        </span>
        <div
          class="absolute inset-0 bg-white/50 translate-y-full group-hover:translate-y-0 transition-transform duration-300"
        >
        </div>
      </a>
    </div>
  </div>

  <a
    id="whatsapp-float"
    href="https://wa.me/59892744211?text=Hola!%20Me%20gustar%C3%ADa%20recibir%20m%C3%A1s%20informaci%C3%B3n%20sobre%20%2BFIT%20y%20reservar%20mi%20prueba%20gratis.%20Gracias!"
    target="_blank"
    rel="noopener noreferrer"
    aria-label="Contactar por WhatsApp"
    class="fixed bottom-5 right-5 z-50 flex items-center gap-2 rounded-full bg-[#25D366] px-4 py-3 text-white shadow-[0_0_20px_rgba(37,211,102,0.35)] transition-all duration-300 hover:scale-105 hover:shadow-[0_0_30px_rgba(37,211,102,0.5)] opacity-0 pointer-events-none"
  >
    <div class="flex h-9 w-9 items-center justify-center rounded-full bg-white/20">
      <MessageCircle class="h-5 w-5" />
    </div>
    <span class="text-sm font-semibold tracking-wide">WhatsApp</span>
  </a>
</header>

<script is:inline>
  const whatsappButton = document.getElementById("whatsapp-float");

  if (whatsappButton) {
    const toggleVisibility = () => {
      const shouldShow = window.scrollY > 220;
      whatsappButton.classList.toggle("opacity-100", shouldShow);
      whatsappButton.classList.toggle("opacity-0", !shouldShow);
      whatsappButton.classList.toggle("pointer-events-none", !shouldShow);
    };

    toggleVisibility();
    window.addEventListener("scroll", toggleVisibility, { passive: true });
  }
</script>
